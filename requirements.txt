torch
numpy
einops
soundfile
huggingface_hub
safetensors
pydantic
uroman

# optional: only used by pipeline.py's _voiced_ok() quality-check retry,
# already try/except-guarded to no-op without it (see pyproject.toml)
# librosa

# evaluation only
# faster-whisper
# jiwer
# datasets
